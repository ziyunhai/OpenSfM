[tool:pytest]
testpaths = opensfm
norecursedirs = opensfm/src
addopts = --doctest-modules
markers =
	slow: long-running integration tests excluded from default coverage runs

[flake8]
ignore = E402

[metadata]
license_file = LICENSE
